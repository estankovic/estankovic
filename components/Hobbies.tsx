import React from "react";
import Section from "@/components/ui/Section";

export default function Hobbies({ children }: { children: React.ReactNode }) {
  return <Section title="Hobbies">{children}</Section>;
}
