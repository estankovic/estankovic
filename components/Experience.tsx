import React from "react";
import Section from "@/components/ui/Section";

export default function Experience({ children }: { children: React.ReactNode }) {
  return <Section title="Experience">{children}</Section>;
}
